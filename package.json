{
  "name": "puppeteer-login-bot",
  "version": "1.0.0",
  "description": "Node JS Puppeteer login bot for common accounting websites.",
  "bin": "./src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alexandre Desroches",
  "license": "ISC",
  "dependencies": {
    "conf": "^10.0.1",
    "puppeteer": "^10.1.0"
  }
}
